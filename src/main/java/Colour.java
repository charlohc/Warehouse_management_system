/**
 * Colour enum class, shows the different colour the item can have
 * (maybe instead of enum, have hex colour wheel that checks real colour).
 */
public enum Colour {
    BLACK,
    WHITE,
    GREY,
    BROWN,
    RED,
    BLUE
}
